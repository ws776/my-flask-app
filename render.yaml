services:
  - type: web
    name: flask-server
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python GeminiServer.py"
    envVars:
      - key: FLASK_ENV
        value: production
